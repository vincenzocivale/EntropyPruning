/checkpoints/
checkpoints/
results/
reports/
/wandb/
__pycache__/
*.pyc
*.h5
.ipynb_checkpoints/
.pytest_cache/
# Runtime logs
logs/
# First real-experiment data and generated run artifacts (large, local-only)
/datasets/
/data/
/experiments/
/manifests/
/splits/
/trident_jobs/
artifacts/
# Canonical WSI store dirs (docs/data_layout.md); belong under $EAF_WSI_ROOT,
# not inside the repo, but ignore them here too in case a script is ever run
# with --data-root pointed at the repo root.
/sources/
/runs/
/catalog/
/views/
/pretraining/
/quarantine/
/cache/
# Offline EAF refactor: caches/ (plural, teacher caches) and archives/ (cold
# pixel archives) are new canonical $EAF_WSI_ROOT dirs (docs/data_layout.md).
/caches/
/archives/
.eaf-tmp/
